import { describe, it } from "vitest";

describe("INFRA-001 + ADMIN-005 + VOICE-006: RLS cross-tenant isolation", () => {
  it.todo("RLS prevents coach A from reading coach B's leads — Plan 02 implements");
  it.todo("Admin service role bypasses RLS for cross-coach queries (server-side only) — Plan 02 implements");
  it.todo("draft_edits table exists with RLS policy scoped to coach_id — Plan 02 implements");
});
