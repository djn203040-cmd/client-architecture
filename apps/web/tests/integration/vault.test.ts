import { describe, it } from "vitest";

describe("INFRA-003: Vault SECURITY DEFINER functions in private schema", () => {
  it.todo("private.store_gmail_tokens exists and is not exposed by PostgREST — Plan 02 implements");
  it.todo("private.get_gmail_tokens exists and is not exposed by PostgREST — Plan 02 implements");
  it.todo("REVOKE ALL from PUBLIC on Vault functions — Plan 02 implements");
});
