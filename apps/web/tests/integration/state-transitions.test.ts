import { describe, it } from "vitest";

describe("STATE-009: State transitions logged to activity timeline", () => {
  it.todo("Lead status change inserts lead_event with event_type=state_changed — Plan 04 implements");
  it.todo("State change event includes previous and new status in payload — Plan 04 implements");
  it.todo("Timeline renders state_changed events with ArrowCircleRight icon — Plan 04 implements");
});
