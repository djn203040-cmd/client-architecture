import { describe, it } from "vitest";

describe("GMAIL-002: OAuth callback stores refresh token to Vault", () => {
  it.todo("OAuth callback with valid code stores refresh_token in Vault — Plan 05 implements");
  it.todo("OAuth callback without refresh_token redirects with vault_failed error — Plan 05 implements");
  it.todo("access_type: offline + prompt: consent enforced in authorize URL — Plan 05 implements");
});
