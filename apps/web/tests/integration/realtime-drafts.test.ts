import { describe, it } from "vitest";

describe("DRAFT-012: Supabase Realtime subscription fires on draft INSERT", () => {
  it.todo("drafts table is in supabase_realtime publication — Plan 02 implements");
  it.todo("Realtime channel receives INSERT event for coach's own drafts — Plan 06 implements");
  it.todo("Realtime channel does NOT receive events for other coach's drafts (RLS) — Plan 06 implements");
});
