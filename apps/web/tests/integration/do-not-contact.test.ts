import { describe, it } from "vitest";

describe("STATE-007: do_not_contact flag persists and blocks sends", () => {
  it.todo("Setting do_not_contact=true persists to database — Plan 04 implements");
  it.todo("Inngest sequence checks do_not_contact before every send attempt — Plan 03 implements");
  it.todo("do_not_contact is a hard gate that overrides all sequence state — Plan 03 implements");
});
