import { describe, it } from "vitest";

describe("INFRA-005: Zod validators", () => {
  it.todo("CreateLeadSchema rejects missing email — Plan 04 implements");
  it.todo("CreateLeadSchema rejects invalid source enum — Plan 04 implements");
  it.todo("CreateLeadSchema accepts valid manual lead — Plan 04 implements");
});
