import { describe, it } from "vitest";

describe("STATE-001: Lead state machine enum", () => {
  it.todo("All 11 lead states present in TS type — Plan 04 implements");
  it.todo("Terminal states (do_not_contact, unsubscribed, converted, closed, bounced) flagged — Plan 04 implements");
});
