import { test } from "@playwright/test";

test.fixme("ADMIN-004: Daniel can invite coach via admin dashboard — Plan 03 implements", async ({ page: _page }) => {
  // Plan 03 implements CreateCoachSheet with inviteCoach() action
});
