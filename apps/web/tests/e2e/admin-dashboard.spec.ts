import { test } from "@playwright/test";

test.fixme("ADMIN-002: Admin sees all coaches, integration health, sequence activity — Plan 07 implements", async ({ page: _page }) => {
  // Plan 07 implements CoachRosterTable and SystemHealthPanel
});
