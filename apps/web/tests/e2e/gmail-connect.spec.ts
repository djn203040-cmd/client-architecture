import { test } from "@playwright/test";

test.fixme("GMAIL-001: Gmail OAuth flow completes and marks integration connected — Plan 05 implements", async ({ page: _page }) => {
  // Plan 05 implements /api/auth/gmail/authorize + callback with Vault storage
});
