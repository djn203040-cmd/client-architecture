import { defineMessages } from "./util";

/** Module 2/3 lock + sell screens (Threshold, Continuation). Filled during the Stage 2 sweep. */
export const modules = defineMessages({
  en: {},
  da: {},
});
