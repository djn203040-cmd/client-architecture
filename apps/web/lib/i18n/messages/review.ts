import { defineMessages } from "./util";

/** External approve-via-link page (/review/[token]) the coach opens from a
 * notification. Filled during the Stage 2 sweep. */
export const review = defineMessages({
  en: {},
  da: {},
});
