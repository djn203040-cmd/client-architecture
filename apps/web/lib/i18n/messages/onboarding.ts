import { defineMessages } from "./util";

/** Onboarding wizard shell + steps (gmail, calendar, booking, voice, sales,
 * notifications, first lead). The language step stays bilingual on its own.
 * Filled during the Stage 2 sweep. */
export const onboarding = defineMessages({
  en: {},
  da: {},
});
