---
plan: 04-00
phase: 4
wave: 0
depends_on: []
files_modified:
  - apps/web/tests/unit/slack-signature.test.ts
  - apps/web/tests/unit/sms-body.test.ts
  - apps/web/tests/unit/autonomous-mode.test.ts
  - apps/web/tests/unit/review-token.test.ts
  - apps/web/tests/integration/notification-dispatcher.test.ts
  - apps/web/tests/integration/draft-followup-cta.test.ts
  - apps/web/tests/integration/draft-hold-cascade.test.ts
  - apps/web/tests/integration/autonomous-mode-b.test.ts
  - apps/web/tests/integration/approve-atomic.test.ts
  - apps/web/tests/integration/bounce-notification.test.ts
  - apps/web/tests/integration/email-channel.test.ts
  - apps/web/tests/integration/slack-channel.test.ts
  - apps/web/tests/integration/whatsapp-channel.test.ts
  - apps/web/tests/integration/webhook-status.test.ts
  - apps/web/tests/integration/slack-interactivity.test.ts
  - apps/web/tests/integration/review-token.test.ts
  - apps/web/tests/integration/notification-preferences-seed.test.ts
  - apps/web/tests/e2e/dashboard-notifications.spec.ts
  - apps/web/tests/e2e/dashboard-approve-flow.spec.ts
  - apps/web/tests/utils/inngest-runner.ts
  - apps/web/tests/utils/supabase-test-client.ts
  - apps/web/tests/utils/mocks/resend.ts
  - apps/web/tests/utils/mocks/twilio.ts
  - apps/web/tests/utils/mocks/slack.ts
autonomous: true
requirements: [DRAFT-001, DRAFT-002, DRAFT-007, DRAFT-008, DRAFT-009, DRAFT-010, DRAFT-011, COMPLY-006, NOTIFY-001, NOTIFY-002, NOTIFY-003, NOTIFY-004, NOTIFY-005, NOTIFY-006, NOTIFY-007, NOTIFY-008]

must_haves:
  truths:
    - "Every Phase 4 requirement has a failing test file referenced in 04-VALIDATION.md"
    - "Every test command in 04-VALIDATION.md resolves to a real file path"
    - "Vendor SDK mocks exist for Resend, Twilio, Slack so channel tests run without network"
  artifacts:
    - path: "apps/web/tests/unit/slack-signature.test.ts"
      provides: "Slack HMAC verify + replay window + timing-safe equality cases"
    - path: "apps/web/tests/unit/sms-body.test.ts"
      provides: "SMS body length cases incl. long lead name"
    - path: "apps/web/tests/unit/autonomous-mode.test.ts"
      provides: "Mode A direct-approved insert path cases"
    - path: "apps/web/tests/unit/review-token.test.ts"
      provides: "Review token generate/verify/expiry/tamper cases"
    - path: "apps/web/tests/integration/notification-dispatcher.test.ts"
      provides: "Parallel fan-out + prefs filtering + channel failure isolation cases"
    - path: "apps/web/tests/integration/draft-followup-cta.test.ts"
      provides: "sleepUntil + followup_count increment cases"
    - path: "apps/web/tests/integration/draft-hold-cascade.test.ts"
      provides: "Second sleepUntil + status->held cases"
    - path: "apps/web/tests/integration/autonomous-mode-b.test.ts"
      provides: "Wake + CAS + already-approved-by-coach cases"
    - path: "apps/web/tests/integration/approve-atomic.test.ts"
      provides: "RPC call + concurrent attempts + not_pending cases"
    - path: "apps/web/tests/integration/bounce-notification.test.ts"
      provides: "Bounce -> dispatcher -> SMS unconditional cases"
    - path: "apps/web/tests/integration/email-channel.test.ts"
      provides: "Resend SDK mocked html+text payload assertion"
    - path: "apps/web/tests/integration/slack-channel.test.ts"
      provides: "Block Kit JSON structure assertion"
    - path: "apps/web/tests/integration/whatsapp-channel.test.ts"
      provides: "Twilio SDK mocked ContentVariables shape assertion"
    - path: "apps/web/tests/integration/webhook-status.test.ts"
      provides: "Twilio + Resend webhook -> notification_log update by external_id"
    - path: "apps/web/tests/integration/slack-interactivity.test.ts"
      provides: "Full button-click -> CAS -> response_url update mock"
    - path: "apps/web/tests/integration/review-token.test.ts"
      provides: "View N times + action consumes nonce cases"
    - path: "apps/web/tests/integration/notification-preferences-seed.test.ts"
      provides: "Channel-connect seed + idempotent re-connect cases"
    - path: "apps/web/tests/e2e/dashboard-notifications.spec.ts"
      provides: "Realtime notification appears in dashboard"
    - path: "apps/web/tests/e2e/dashboard-approve-flow.spec.ts"
      provides: "Approve+Next on real DB seeded with draft"
    - path: "apps/web/tests/utils/inngest-runner.ts"
      provides: "In-process Inngest step runner for tests"
    - path: "apps/web/tests/utils/supabase-test-client.ts"
      provides: "Service-role test client + RLS impersonation helper"
    - path: "apps/web/tests/utils/mocks/resend.ts"
      provides: "Resend SDK mock"
    - path: "apps/web/tests/utils/mocks/twilio.ts"
      provides: "Twilio SDK mock"
    - path: "apps/web/tests/utils/mocks/slack.ts"
      provides: "Slack WebClient mock"
  key_links:
    - from: "tests/integration/*"
      to: "tests/utils/inngest-runner.ts + tests/utils/supabase-test-client.ts"
      via: "import"
      pattern: "from \"@/tests/utils/(inngest-runner|supabase-test-client)\""
    - from: "tests/integration/(email|slack|whatsapp)-channel.test.ts"
      to: "tests/utils/mocks/*"
      via: "vi.mock() of resend / twilio / @slack/web-api"
      pattern: "vi\\.mock\\((\"resend|twilio|@slack/web-api\")"
---

<objective>
Scaffold all Phase 4 test files referenced in 04-VALIDATION.md so every downstream task has a failing test to drive against. No production code — only test stubs (RED state), shared utilities, and vendor SDK mocks. Each test stub MUST compile, MUST be runnable via the exact command listed in 04-VALIDATION.md, and MUST fail with a clear "not implemented" message.

Purpose: Honor the Nyquist Rule from `<task_breakdown>` — every Phase 4 `<verify>` automated command resolves to a real file. Unblocks parallel execution by giving each downstream plan a precise red test to flip green.

Output: 22 test files + 5 shared utility/mock files. Vitest 4.1.5 + Playwright 1.59.1 already installed (per `apps/web/package.json`). Zero new framework dependencies. Set `nyquist_compliant: true` in 04-VALIDATION.md frontmatter as the final step.
</objective>

<execution_context>
@/Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.claude/get-shit-done/workflows/execute-plan.md
@/Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-approval-channels/04-CONTEXT.md
@.planning/phases/04-approval-channels/04-RESEARCH.md
@.planning/phases/04-approval-channels/04-VALIDATION.md
@.planning/phases/04-approval-channels/04-PATTERNS.md
@CLAUDE.md
@apps/web/vitest.config.ts
@apps/web/playwright.config.ts
@apps/web/package.json

<interfaces>
<!-- Vitest test stub shape (use this exact pattern for every test stub) -->
```typescript
import { describe, it, expect } from "vitest";

describe("<feature> (Phase 4)", () => {
  it.todo("does the thing — see plan 04-XX-PLAN.md");
  // OR (for an immediately-failing stub):
  it("does the thing", () => {
    throw new Error("not implemented — see plan 04-XX-PLAN.md");
  });
});
```

<!-- Playwright stub shape -->
```typescript
import { test, expect } from "@playwright/test";

test.fixme("dashboard notification appears via Realtime", async ({ page }) => {
  // Implementation in plan 04-02-PLAN.md
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create shared test utilities + vendor SDK mocks</name>
  <files>apps/web/tests/utils/inngest-runner.ts, apps/web/tests/utils/supabase-test-client.ts, apps/web/tests/utils/mocks/resend.ts, apps/web/tests/utils/mocks/twilio.ts, apps/web/tests/utils/mocks/slack.ts</files>
  <read_first>
    apps/web/inngest/client.ts (for Inngest event/client shape)
    apps/web/lib/supabase/admin.ts (for adminClient shape)
    apps/web/package.json (verify vitest version 4.1.5)
    apps/web/tsconfig.json (path aliases)
  </read_first>
  <action>
Create five utility files. All are test-only — no `"server-only"` import (these run in test environment).

1. `apps/web/tests/utils/inngest-runner.ts` — Export `runInngestStep(fn, event)` that synchronously invokes an Inngest function definition for tests. Stub `step.run`, `step.sleepUntil` (resolves immediately), `step.sendEvent` (records into an in-memory queue exposed via `getSentEvents()`). Reset queue between tests via `resetInngestQueue()`. Use plain JS objects — no real Inngest dev server required.

2. `apps/web/tests/utils/supabase-test-client.ts` — Export `createTestClient(coachId?: string)` returning a thin wrapper around `@supabase/supabase-js` configured against `process.env.SUPABASE_URL` + `process.env.SUPABASE_SERVICE_ROLE_KEY`. Export `withRlsAs(coachId)` helper that sets `request.jwt.claim.sub` via `auth.setSession` for RLS-scoped reads. Include `seedDraft({ coachId, leadId, status, scheduledSendAt? })` and `seedCoach({ id?, email })` helpers used by integration tests.

3. `apps/web/tests/utils/mocks/resend.ts` — Export `mockResend` with `emails.send({...})` returning `{ data: { id: "test-email-id" }, error: null }` by default. Capture last call args via `mockResend.emails.send.mock.calls`. Use `vi.fn()`. Also export `installResendMock()` calling `vi.mock("resend", () => ({ Resend: vi.fn(() => mockResend) }))`.

4. `apps/web/tests/utils/mocks/twilio.ts` — Export `mockTwilioClient` with `messages.create({...})` returning `{ sid: "test-message-sid" }`. Export `validateRequest` mock (defaults to `() => true`). Export `installTwilioMock()` calling `vi.mock("twilio", () => ({ default: vi.fn(() => mockTwilioClient), validateRequest: ... }))`.

5. `apps/web/tests/utils/mocks/slack.ts` — Export `mockWebClient` with `chat.postMessage`, `chat.update`, `views.open`, `oauth.v2.access` each as `vi.fn()` returning sensible defaults (`{ ok: true, ts: "test-ts", channel: "D-test" }`, `{ ok: true }`, `{ ok: true, access_token: "xoxb-test", bot_user_id: "B1", team: { id: "T1" }, authed_user: { id: "U1" } }`). Export `installSlackMock()` calling `vi.mock("@slack/web-api", () => ({ WebClient: vi.fn(() => mockWebClient) }))`.

Per CLAUDE.md `INFRA-006`: TypeScript strict, no `any`. Use `unknown` + type guards where the shape is dynamic.

Per CLAUDE.md `COMPLY-009`: never log full bot tokens or draft bodies — the mocks return `"xoxb-test"` literals only.
  </action>
  <verify>
    <automated>pnpm --filter web exec tsc --noEmit -p apps/web/tsconfig.json 2>&amp;1 | grep -E "tests/utils" || echo "OK: no TS errors in tests/utils"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/tests/utils/inngest-runner.ts` exists and exports `runInngestStep`, `getSentEvents`, `resetInngestQueue`
    - `apps/web/tests/utils/supabase-test-client.ts` exists and exports `createTestClient`, `seedDraft`, `seedCoach`
    - `apps/web/tests/utils/mocks/{resend,twilio,slack}.ts` each exist and export `install*Mock()` + the mock object
    - `grep -c "vi.fn()" apps/web/tests/utils/mocks/slack.ts` returns ≥ 4 (postMessage, update, open, oauth.v2.access)
    - `tsc --noEmit` reports zero errors in `tests/utils/**`
    - No `any` anywhere in the new files: `grep -n ": any" apps/web/tests/utils/**/*.ts` returns no output
  </acceptance_criteria>
  <done>Five utility files created, type-check clean, mocks usable by `vi.mock()` calls in subsequent test stubs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold 4 unit test files (RED state)</name>
  <files>apps/web/tests/unit/slack-signature.test.ts, apps/web/tests/unit/sms-body.test.ts, apps/web/tests/unit/autonomous-mode.test.ts, apps/web/tests/unit/review-token.test.ts</files>
  <read_first>
    .planning/phases/04-approval-channels/04-VALIDATION.md (test commands map)
    .planning/phases/04-approval-channels/04-RESEARCH.md lines 380-397 (Slack sig verify shape)
    .planning/phases/04-approval-channels/04-RESEARCH.md lines 680-690 (SMS Pitfall 5)
    .planning/phases/04-approval-channels/04-RESEARCH.md lines 787-822 (review token shape)
    apps/web/lib/unsubscribe-token.ts (analog for review-token tests)
  </read_first>
  <action>
Create four Vitest unit test files. Each file MUST use the test names referenced by 04-VALIDATION.md `-t "..."` flags so the existing commands resolve.

1. `apps/web/tests/unit/slack-signature.test.ts` — Test cases:
   - `it("verifies a valid v0 signature with current timestamp")`
   - `it("rejects timestamp older than 5 minutes (replay window)")`
   - `it("rejects modified body (timing-safe mismatch)")`
   - `it("rejects mismatched signing secret")`
   Import target: `verifySlackSignature` from `@/lib/slack/signature` (does not exist yet — plan 04-04 creates it). Use `it.todo()` for now or throw `Error("not implemented — see plan 04-04-PLAN.md")` so the test FAILS (not pending).

2. `apps/web/tests/unit/sms-body.test.ts` — Test cases:
   - `it("builds initial SMS body under 160 chars for typical lead name")`
   - `it("builds follow-up SMS body under 160 chars")`
   - `it("long lead name", ...)` — exactly this test name per 04-VALIDATION Pitfall-5 row — asserts truncation to ≤30 chars and total body ≤160. Test input: `lead_name = "x".repeat(50)`, `short_link = "https://app.sonorous.com/r/" + "a".repeat(22)`.
   - `it("builds hard-bounce SMS body")`
   Import target: `buildSmsBody` from `@/lib/notifications/channels/sms` (plan 04-05).

3. `apps/web/tests/unit/autonomous-mode.test.ts` — Test cases:
   - `it("creates a draft with status='approved' when coach is in Mode A")`
   - `it("creates a draft with status='pending' when coach is in Mode B")`
   - `it("creates a draft with status='pending' when coach is in Manual mode")`
   - `it("requires high-friction phrase confirmation before persisting Mode A")` — asserts the API rejects `{ mode: 'mode_a' }` without `confirmation_phrase: 'send without review'`.
   Import target: `createDraftForCoach`, `setAutonomousMode` (plan 04-06).

4. `apps/web/tests/unit/review-token.test.ts` — Test cases (mirrors 04-PATTERNS.md analog):
   - `it("generates a token with embedded payload")`
   - `it("verifies a valid token and returns payload")`
   - `it("rejects expired token (exp < now)")`
   - `it("rejects tampered payload (HMAC mismatch)")`
   - `it("rejects malformed token (missing separator)")`
   Import target: `generateReviewToken`, `verifyReviewToken` from `@/lib/review-token` (plan 04-03).

Each test uses `Error("not implemented — see plan 04-XX-PLAN.md")` for the body so `vitest run` produces FAIL output (not skipped). This makes the RED state visible in CI.

Use `import { describe, it, expect } from "vitest"`. Set `JWT_REVIEW_SECRET=test-secret` and `SLACK_SIGNING_SECRET=test-secret` via `beforeAll` for the tests that need env vars.
  </action>
  <verify>
    <automated>pnpm --filter web vitest run tests/unit/slack-signature.test.ts tests/unit/sms-body.test.ts tests/unit/autonomous-mode.test.ts tests/unit/review-token.test.ts 2>&amp;1 | grep -E "FAIL|not implemented" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist under `apps/web/tests/unit/`
    - `grep -c "not implemented" apps/web/tests/unit/sms-body.test.ts` ≥ 1
    - `grep -c 'it("long lead name"' apps/web/tests/unit/sms-body.test.ts` returns 1 (exact test name per VALIDATION)
    - `vitest run` returns non-zero exit code (all tests are RED) but does NOT crash on import errors
    - Test files reference targets via `@/lib/...` path aliases that resolve to non-existent modules — must use dynamic `await import()` inside `it()` bodies OR top-level imports guarded so the file at least compiles (use the latter; the missing module surfaces as a runtime "not implemented" error)
  </acceptance_criteria>
  <done>Four unit test files exist, all tests RED with clear "not implemented" markers pointing to the correct downstream plan.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Scaffold 13 integration test files + 2 E2E specs (RED state) + flip nyquist_compliant flag</name>
  <files>apps/web/tests/integration/notification-dispatcher.test.ts, apps/web/tests/integration/draft-followup-cta.test.ts, apps/web/tests/integration/draft-hold-cascade.test.ts, apps/web/tests/integration/autonomous-mode-b.test.ts, apps/web/tests/integration/approve-atomic.test.ts, apps/web/tests/integration/bounce-notification.test.ts, apps/web/tests/integration/email-channel.test.ts, apps/web/tests/integration/slack-channel.test.ts, apps/web/tests/integration/whatsapp-channel.test.ts, apps/web/tests/integration/webhook-status.test.ts, apps/web/tests/integration/slack-interactivity.test.ts, apps/web/tests/integration/review-token.test.ts, apps/web/tests/integration/notification-preferences-seed.test.ts, apps/web/tests/e2e/dashboard-notifications.spec.ts, apps/web/tests/e2e/dashboard-approve-flow.spec.ts, .planning/phases/04-approval-channels/04-VALIDATION.md</files>
  <read_first>
    .planning/phases/04-approval-channels/04-VALIDATION.md (exact test names and -t filters)
    .planning/phases/04-approval-channels/04-RESEARCH.md lines 900-955 (per-req test map)
    apps/web/tests/utils/inngest-runner.ts (from Task 1)
    apps/web/tests/utils/supabase-test-client.ts (from Task 1)
  </read_first>
  <action>
Create 13 integration test stubs + 2 E2E specs. Each integration test uses `describe(...)` with the file's domain name and includes the EXACT `-t "..."` filter strings from 04-VALIDATION.md so `vitest run -t "..."` resolves.

Required `-t` filter strings (verbatim):
- `notification-dispatcher.test.ts`: `"fires draft_ready"`, `"fans out to all enabled"`, `"parallel"`
- `approve-atomic.test.ts`: `"concurrent attempts"`
- `slack-interactivity.test.ts`: `"approve flow"`
- `review-token.test.ts`: `"read-only does not consume nonce"`

For each integration file:
- Import `runInngestStep`, `getSentEvents`, `resetInngestQueue` from `@/tests/utils/inngest-runner` (when applicable)
- Import `createTestClient`, `seedDraft`, `seedCoach` from `@/tests/utils/supabase-test-client` (when applicable)
- Import vendor mocks from `@/tests/utils/mocks/*` and call `installResendMock()` / `installSlackMock()` / `installTwilioMock()` at top of file
- Use `beforeEach` to reset Inngest queue and clear mock call history
- Body: `throw new Error("not implemented — see plan 04-XX-PLAN.md")` referencing the correct downstream plan:
  - notification-dispatcher, bounce-notification, draft-followup-cta, draft-hold-cascade, notification-preferences-seed → plan 04-07
  - approve-atomic → plan 04-01
  - autonomous-mode-b → plan 04-06
  - email-channel, review-token (integration), webhook-status (Resend half) → plan 04-03
  - slack-channel, slack-interactivity, webhook-status (Slack half) → plan 04-04
  - whatsapp-channel, webhook-status (Twilio half) → plan 04-05

For E2E specs (`apps/web/tests/e2e/`):
- `dashboard-notifications.spec.ts` — use `test.fixme("dashboard notification appears via Realtime", ...)` referencing plan 04-02 + 04-07
- `dashboard-approve-flow.spec.ts` — `test.fixme("Approve+Next on seeded draft completes Gmail send path", ...)` referencing plan 04-02

After creating all 15 files, edit `.planning/phases/04-approval-channels/04-VALIDATION.md` frontmatter:
- Change `nyquist_compliant: false` → `nyquist_compliant: true`
- Change `wave_0_complete: false` → `wave_0_complete: true`
- Change `status: draft` → `status: approved`
- Append signed-off line at end of file: `**Approval:** scaffolded by 04-00 — 2026-05-20`

Per CLAUDE.md TypeScript strict — no `any`. Use `unknown` and narrow at runtime.
  </action>
  <verify>
    <automated>pnpm --filter web vitest run tests/integration/ 2>&amp;1 | grep -E "Tests.*failed" | head -3 &amp;&amp; grep -c "nyquist_compliant: true" .planning/phases/04-approval-channels/04-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - All 13 integration test files exist
    - Both E2E spec files exist
    - `grep -l "not implemented" apps/web/tests/integration/*.test.ts | wc -l` returns 13
    - `grep -c 'it("concurrent attempts"' apps/web/tests/integration/approve-atomic.test.ts` returns 1
    - `grep -c 'it("approve flow"' apps/web/tests/integration/slack-interactivity.test.ts` returns 1
    - `grep -c "fires draft_ready" apps/web/tests/integration/notification-dispatcher.test.ts` returns ≥ 1
    - `grep -c "read-only does not consume nonce" apps/web/tests/integration/review-token.test.ts` returns ≥ 1
    - `pnpm --filter web vitest run tests/integration/` exits non-zero (RED state) but does not crash on import
    - `grep -c "nyquist_compliant: true" .planning/phases/04-approval-channels/04-VALIDATION.md` returns 1
    - `grep -c "wave_0_complete: true" .planning/phases/04-approval-channels/04-VALIDATION.md` returns 1
  </acceptance_criteria>
  <done>22 test files + 5 utility files exist, all RED with clear "not implemented" markers, VALIDATION.md flipped to nyquist_compliant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-runner → fs | Tests write/read files only inside `apps/web/tests/` |
| test-runner → mocked SDKs | No outbound network — Resend/Twilio/Slack SDKs are vi.mock'd |
| test-runner → Supabase | Integration tests CAN hit a real Supabase test project via service role; gated by env vars |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00-01 | I | tests/utils/mocks/slack.ts | accept | Mock token literal is `"xoxb-test"` — clearly fake; cannot be confused with real bot token |
| T-04-00-02 | I | tests/utils/supabase-test-client.ts | mitigate | Test client reads `SUPABASE_SERVICE_ROLE_KEY` from env only; never hard-coded; CI test env uses staging Supabase project |
| T-04-00-03 | T | test stubs throw Error("not implemented") | accept | RED state is the intended product of Wave 0; downstream plans flip GREEN |
</threat_model>

<verification>
- `pnpm --filter web exec tsc --noEmit` clean (no test-file type errors)
- `pnpm --filter web vitest run tests/unit/` exits non-zero (expected RED) without import crashes
- `pnpm --filter web vitest run tests/integration/` exits non-zero (expected RED) without import crashes
- `pnpm --filter web playwright test tests/e2e/ --list` lists both new specs as `fixme`
- 04-VALIDATION.md `nyquist_compliant: true` set
</verification>

<success_criteria>
- 22 test files created with exact test names matching 04-VALIDATION.md commands
- 5 utility/mock files created and importable
- All tests RED with "not implemented — see plan 04-XX-PLAN.md" markers
- 04-VALIDATION.md frontmatter flipped to nyquist_compliant + wave_0_complete + status: approved
- Zero TypeScript errors in `tests/` directory
- Zero `any` in test code
- No real network calls (vendor SDKs mocked via vi.mock)
</success_criteria>

<output>
After completion, create `.planning/phases/04-approval-channels/04-00-SUMMARY.md`
</output>
