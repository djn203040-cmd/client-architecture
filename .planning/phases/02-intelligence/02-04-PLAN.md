---
phase: 02-intelligence
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - apps/web/app/api/drafts/[id]/regenerate/route.ts
  - apps/web/components/drafts/DraftCard.tsx
autonomous: true
requirements: [AI-010, AI-011, AI-007]
user_setup: []

must_haves:
  truths:
    - "Coach can regenerate any draft with one click"
    - "Regeneration reuses the same context and produces a fresh draft body"
    - "The regenerated draft replaces the current draft in place via Realtime"
    - "DraftCard shows an amber confidence badge when the draft confidence_level is 'low'"
  artifacts:
    - path: "apps/web/app/api/drafts/[id]/regenerate/route.ts"
      provides: "POST one-click draft regeneration"
      exports: ["POST"]
    - path: "apps/web/components/drafts/DraftCard.tsx"
      provides: "Regen button + amber confidence badge"
      contains: "Regenerate"
  key_links:
    - from: "apps/web/components/drafts/DraftCard.tsx"
      to: "/api/drafts/[id]/regenerate"
      via: "fetch on regen button click"
      pattern: "regenerate"
    - from: "apps/web/app/api/drafts/[id]/regenerate/route.ts"
      to: "packages/ai-engine generateDraft"
      via: "import from @client/ai-engine"
      pattern: "generateDraft"
---

<objective>
Add one-click draft regeneration: a regenerate route that reuses the same context to make a fresh Anthropic call and replaces the draft body in place, plus a Regenerate button and an upgraded amber confidence badge on the existing DraftCard.

Purpose: Coaches need a fast way to get a different draft when the first one is not right, and a clear visual signal when the voice model is too thin to trust. This depends on the AI engine (02-03) being complete.
Output: The regenerate route and DraftCard extensions.
</objective>

<execution_context>
@/Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.claude/get-shit-done/workflows/execute-plan.md
@/Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-intelligence/02-CONTEXT.md
@.planning/phases/02-intelligence/02-UI-SPEC.md
@.planning/phases/02-intelligence/02-PATTERNS.md
@CLAUDE.md
@.planning/phases/02-intelligence/02-03-SUMMARY.md

<interfaces>
<!-- From 02-03: generateDraft is implemented and exported -->
import { generateDraft } from '@client/ai-engine';
// generateDraft(params: DraftGenerationParams): Promise<{ body, confidenceLevel, truncationLog, qualityFlags } | null>

<!-- drafts table -->
drafts: id, coach_id, lead_id, body, status, touchpoint_index, confidence_level ('high'|'low'), generation_context, created_at

<!-- Existing DraftCard confidence badge (DraftCard.tsx ~lines 104-107) — REPLACE this -->
{draft.confidence_level === "low" && (
  <span className="... bg-secondary text-secondary-foreground">Low voice confidence</span>
)}

<!-- Existing DraftCard action pattern (~lines 33-45) — copy for regen -->
async function setStatus(status, body?) {
  const r = await fetch(`/api/drafts/${draft.id}`, { method: "PATCH", ... });
  if (!r.ok) { toast.error(...); return; }
  toast.success(...); onAdvance();
}

<!-- Amber confidence color (UI-SPEC Color section) -->
bg: oklch(72% 0.12 70) light / oklch(25% 0.08 65) dark
text: oklch(40% 0.10 65) light / oklch(85% 0.08 65) dark
icon: Phosphor WarningCircle size-3, no border, subtle fill only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft regeneration route</name>
  <files>apps/web/app/api/drafts/[id]/regenerate/route.ts</files>
  <read_first>
    - apps/web/app/api/leads/[id]/route.ts (dynamic async params + PATCH ownership pattern)
    - apps/web/app/api/drafts/generate/route.ts (02-03 — fire-and-forget generation + DraftGenerationParams assembly)
    - .planning/phases/02-intelligence/02-PATTERNS.md (regenerate route section, lines 300-320)
  </read_first>
  <action>
    Create `apps/web/app/api/drafts/[id]/regenerate/route.ts` — `POST` with dynamic async params `{ params: Promise<{ id: string }> }`; `const { id } = await params`. Coach-scoped auth via `createClient()` + `getUser()` (401 gate).

    Verify ownership: `supabase.from("drafts").select("coach_id, lead_id, touchpoint_index").eq("id", id).maybeSingle()` — 404 if not found, 403 if `coach_id !== user.id`.

    Reuse the SAME context the original draft was generated from (AI-011): re-load the lead (current `lead_status`), the coach's current `voice_model`, the lead's transcripts (oldest-first), prior sent emails (conversation history), coach notes, and `ai_summary` — exactly the same `DraftGenerationParams` assembly the generate route uses. Use the existing `touchpoint_index` from the draft row so the regen stays on the same touchpoint.

    Set the draft row to `status: 'generating'`, then fire-and-forget (RESEARCH.md Pattern 6): call `generateDraft(params)` from `@client/ai-engine`; on success UPDATE the same draft row in place with the new `body`, `status: 'pending'`, `confidence_level`, and `generation_context` (D-23 — replace the current draft, do not create a new row); on failure set `status: 'error'`. Return `{ draftId: id, status: 'generating' }` immediately (within ~200ms).

    Factor the `DraftGenerationParams` assembly into a shared helper if it is duplicated from the generate route — extract to `apps/web/lib/drafts/build-params.ts` and import it in both routes (avoids drift). If extraction is clean, do it; otherwise inline carefully and match the generate route exactly.
  </action>
  <verify>
    <automated>pnpm --filter apps/web type-check && pnpm --filter apps/web build</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/api/drafts/[id]/regenerate/route.ts contains `export async function POST` and `params: Promise<`
    - File contains `generateDraft` imported from the ai-engine package
    - File contains an ownership check (`coach_id` compared to `user.id`) returning 403
    - File sets `status: 'generating'` then updates the SAME draft id (no `.insert(` into drafts)
    - File returns `status: 'generating'` immediately and uses a fire-and-forget `.catch(`
    - `pnpm --filter apps/web type-check` exits 0
    - `pnpm --filter apps/web build` exits 0
  </acceptance_criteria>
  <done>POST /api/drafts/[id]/regenerate reuses the original context, makes a fresh Anthropic call, and replaces the existing draft body in place.</done>
</task>

<task type="auto">
  <name>Task 2: DraftCard regen button + amber confidence badge</name>
  <files>apps/web/components/drafts/DraftCard.tsx</files>
  <read_first>
    - apps/web/components/drafts/DraftCard.tsx (existing file — full read; confidence badge ~104-107, action pattern ~33-45, icon imports ~5-10)
    - apps/web/components/drafts/draft-realtime.tsx (Realtime patch-in-place pattern)
    - .planning/phases/02-intelligence/02-UI-SPEC.md (component 9: DraftCard additions, Color section, Copywriting Contract)
  </read_first>
  <action>
    Modify `apps/web/components/drafts/DraftCard.tsx` — keep all existing approve/skip/hold/edit behavior intact.

    Add a `regenerate()` async function following the existing `setStatus` pattern: `fetch(`/api/drafts/${draft.id}/regenerate`, { method: "POST" })`; on `!r.ok` `toast.error("Regeneration failed. Try again.")`; on success `toast.success("Regenerating draft...")`. No local state change for the body — the draft updates via the existing Realtime subscription (the regenerate route patches the same row, so DraftCard re-renders in place, no remount).

    Add a Regenerate button in the DraftCard header alongside the existing edit (`PencilSimple`) button: ghost icon button, Phosphor `ArrowsClockwise size-4`, `min-h-[44px] min-w-[44px]`, `aria-label="Regenerate draft"`. While regenerating (track a local `isRegenerating` state set on click, cleared when Realtime reports the new `pending` status) spin the icon (`animate-spin`), disable the button, and show a shadcn `Tooltip` "Generating new draft...". Import `ArrowsClockwise` from `@phosphor-icons/react`.

    Replace the existing low-confidence badge (currently `bg-secondary text-secondary-foreground`, text "Low voice confidence") with the amber treatment from the UI-SPEC Color section: warm amber fill (`oklch(72% 0.12 70)` bg light / `oklch(25% 0.08 65)` dark; `oklch(40% 0.10 65)` text light / `oklch(85% 0.08 65)` dark — express via the project's Tailwind amber/confidence tokens), no border, Phosphor `WarningCircle size-3` icon inline before the text, new text "Voice model needs more examples". Render only when `draft.confidence_level === "low"`. Keep it color-independent: icon + text, not color alone.

    Keep the file under 200 lines — if adding the button + tooltip pushes it over, extract the regen button into a small `DraftRegenButton.tsx` sub-component in the same directory.
  </action>
  <verify>
    <automated>pnpm --filter apps/web type-check && pnpm --filter apps/web build</automated>
  </verify>
  <acceptance_criteria>
    - DraftCard.tsx (or an extracted sub-component) contains a fetch to a path ending `/regenerate` with `method: "POST"`
    - DraftCard.tsx contains `ArrowsClockwise` and `aria-label="Regenerate draft"`
    - DraftCard.tsx contains the new badge text `Voice model needs more examples` and `WarningCircle`
    - DraftCard.tsx no longer contains the old badge text `Low voice confidence`
    - The badge renders conditionally on `confidence_level === "low"`
    - Existing approve/skip/hold/edit handlers are unchanged (grep still finds `setStatus`)
    - DraftCard.tsx is under 200 lines (or the regen button was extracted)
    - `pnpm --filter apps/web type-check` exits 0
    - `pnpm --filter apps/web build` exits 0
  </acceptance_criteria>
  <done>DraftCard has a one-click Regenerate button with a spinning/disabled state and an amber confidence badge; existing actions intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coach browser -> /api/drafts/[id]/regenerate | Coach-initiated regeneration request |
| regenerate route -> Anthropic API | Lead context re-sent to third-party LLM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Spoofing | Unauthenticated regenerate call | mitigate | `supabase.auth.getUser()` 401 gate |
| T-02-21 | Elevation of Privilege | Coach regenerates another coach's draft | mitigate | Ownership check: `draft.coach_id === user.id` else 403 |
| T-02-22 | Tampering | Draft regenerated for a now-terminal lead state | mitigate | generateDraft re-checks `isHardBlocked` on the freshly loaded lead_status and returns null; route leaves the draft unchanged |
| T-02-23 | Denial of Service | Rapid repeated regen requests run up Anthropic cost | accept | Low volume at 5-10 coaches; AI-SPEC defers rate limiting (20 drafts/coach/day) to scale-up — re-evaluate then |
</threat_model>

<verification>
- `pnpm --filter apps/web type-check` clean
- `pnpm --filter apps/web build` succeeds
- Manual: clicking Regenerate on a draft produces a visibly different body in place; the amber badge shows on a low-confidence draft
</verification>

<success_criteria>
- One-click regeneration reuses context, makes a fresh call, replaces the draft in place
- DraftCard shows an amber confidence badge when confidence_level is 'low'
- All requirement IDs covered: AI-010, AI-011, AI-007
</success_criteria>

<output>
After completion, create `.planning/phases/02-intelligence/02-04-SUMMARY.md`
</output>
