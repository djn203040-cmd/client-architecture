---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - .gitignore
  - .env.example
  - .nvmrc
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/tsconfig.json
  - apps/web/middleware.ts
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
  - apps/web/app/globals.css
  - apps/web/components.json
  - apps/web/postcss.config.mjs
  - apps/web/tailwind.config.ts
  - apps/web/eslint.config.mjs
  - apps/web/lib/supabase/browser.ts
  - apps/web/lib/supabase/server.ts
  - apps/web/lib/supabase/admin.ts
  - apps/web/inngest/client.ts
  - apps/web/app/api/inngest/route.ts
  - apps/web/vitest.config.ts
  - apps/web/playwright.config.ts
  - apps/web/tests/unit/validators.test.ts
  - apps/web/tests/unit/state-machine.test.ts
  - apps/web/tests/unit/scope-validation.test.ts
  - apps/web/tests/unit/invalid-grant.test.ts
  - apps/web/tests/integration/rls.test.ts
  - apps/web/tests/integration/vault.test.ts
  - apps/web/tests/integration/gmail-oauth.test.ts
  - apps/web/tests/integration/vault-storage.test.ts
  - apps/web/tests/integration/do-not-contact.test.ts
  - apps/web/tests/integration/state-transitions.test.ts
  - apps/web/tests/integration/realtime-drafts.test.ts
  - apps/web/tests/integration/ratelimit.test.ts
  - apps/web/tests/e2e/lead-create.spec.ts
  - apps/web/tests/e2e/lead-profile.spec.ts
  - apps/web/tests/e2e/lead-timeline.spec.ts
  - apps/web/tests/e2e/lead-notes.spec.ts
  - apps/web/tests/e2e/lead-list.spec.ts
  - apps/web/tests/e2e/admin-access.spec.ts
  - apps/web/tests/e2e/admin-dashboard.spec.ts
  - apps/web/tests/e2e/invite-coach.spec.ts
  - apps/web/tests/e2e/gmail-connect.spec.ts
  - apps/web/tests/e2e/health-card.spec.ts
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - packages/shared/src/types/index.ts
  - packages/shared/src/validators/index.ts
  - packages/shared/src/validators/lead.ts
  - packages/shared/src/constants/events.ts
  - packages/database/package.json
  - packages/database/tsconfig.json
  - packages/database/src/index.ts
  - packages/database/src/types.ts
  - packages/ai-engine/package.json
  - packages/ai-engine/tsconfig.json
  - packages/ai-engine/src/index.ts
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - INFRA-002
  - INFRA-006
  - INFRA-007
  - INFRA-008
  - INFRA-009
  - INFRA-010
  - COMPLY-009
  - COMPLY-010
must_haves:
  truths:
    - "Repo builds end-to-end via `turbo build` with zero errors"
    - "TypeScript strict mode is enforced; no `any` permitted"
    - "Three workspace packages (shared, database, ai-engine) resolve via @client/* aliases"
    - "ai-engine cannot be imported from a client component (runtime guard + browser field)"
    - "/api/inngest route exports GET, POST, PUT with maxDuration=300"
    - "Test scaffolding exists for all Phase 1 tests (Vitest + Playwright configs)"
    - "CI fails when NEXT_PUBLIC_ is prefixed onto a service role var"
    - "Tailwind v4 + shadcn/ui new-york preset wired with OKLCH tokens from UI-SPEC"
  artifacts:
    - path: "package.json"
      provides: "Root workspace manifest"
      contains: "private.*true"
    - path: "pnpm-workspace.yaml"
      provides: "pnpm workspace config"
      contains: "apps/*"
    - path: "turbo.json"
      provides: "Turborepo task pipeline"
      contains: "build"
    - path: "apps/web/next.config.ts"
      provides: "Next.js config with transpilePackages"
      contains: "transpilePackages"
    - path: "apps/web/middleware.ts"
      provides: "Supabase auth refresh middleware"
      contains: "getUser"
    - path: "apps/web/app/api/inngest/route.ts"
      provides: "Inngest serve scaffold"
      contains: "maxDuration"
    - path: "packages/ai-engine/src/index.ts"
      provides: "ai-engine server-only guard"
      contains: "typeof window"
    - path: "apps/web/vitest.config.ts"
      provides: "Vitest configuration"
    - path: "apps/web/playwright.config.ts"
      provides: "Playwright configuration"
    - path: ".github/workflows/ci.yml"
      provides: "CI gate including service-role grep check"
      contains: "NEXT_PUBLIC_"
  key_links:
    - from: "apps/web"
      to: "packages/shared, packages/database, packages/ai-engine"
      via: "workspace:* dependencies + transpilePackages"
      pattern: "workspace:\\*"
    - from: "apps/web/middleware.ts"
      to: "@supabase/ssr createServerClient"
      via: "cookie-based auth refresh"
      pattern: "createServerClient"
---

<objective>
Establish the Turborepo + pnpm monorepo skeleton that all subsequent Phase 1 plans build into. This is Wave 0 + Wave 1 combined: it creates the test infrastructure (Wave 0) AND the application scaffolding (Wave 1) in one focused plan because no other plan can begin until these files exist.

Purpose: Plans 02–07 depend on the workspace structure, the @client/shared types and validators, the Supabase client factories, and the test scaffolding. Without this plan landing first, every other plan is blocked.

Output:
- Monorepo structure: apps/web + packages/{shared,database,ai-engine}
- TypeScript strict, ESLint, Tailwind v4 + shadcn/ui new-york preset, Geist fonts, Phosphor icons, Framer Motion installed
- @supabase/ssr browser/server clients + service-role admin client
- Inngest serve scaffold at /api/inngest with maxDuration = 300
- Instagram webhook verification scaffold at /api/webhooks/instagram (INFRA-010)
- Vitest + Playwright configs with empty test stubs for every Phase 1 test
- CI workflow with NEXT_PUBLIC_ service-role grep check + ESLint no-console rule
- ai-engine package with server-only runtime guard + browser field
- .env.example documenting every required env var
</objective>

<execution_context>
@/Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.claude/get-shit-done/workflows/execute-plan.md
@/Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/CLAUDE.md
@/Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.planning/PROJECT.md
@/Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.planning/ROADMAP.md
@/Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.planning/STATE.md
@/Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.planning/REQUIREMENTS.md
@/Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.planning/phases/01-foundation/01-RESEARCH.md
@/Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.planning/phases/01-foundation/01-UI-SPEC.md
@/Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.planning/phases/01-foundation/01-VALIDATION.md

<interfaces>
<!-- Verbatim values from RESEARCH.md — do NOT improvise versions, paths, or names -->

Pinned package versions (from RESEARCH.md "Standard Stack"):
- next: 16.2.4
- @supabase/supabase-js: 2.105.3
- @supabase/ssr: 0.10.2
- inngest: 4.2.6
- googleapis: 171.4.0
- @upstash/ratelimit: 2.0.8
- @upstash/redis: 1.37.0
- zod: 4.4.3
- tailwindcss: 4.2.4
- framer-motion: 12.38.0
- @phosphor-icons/react: 2.1.10
- shadcn (CLI): 4.7.0  (run via `pnpm dlx shadcn@4.7.0 init --style new-york`)
- vitest: 4.1.5
- @playwright/test: 1.59.1
- turbo: 2.9.9

Workspace package names (used in apps/web/package.json):
- @client/shared
- @client/database
- @client/ai-engine

Supabase client factories (verbatim signatures from RESEARCH.md Pattern 3):

```typescript
// apps/web/lib/supabase/browser.ts
import { createBrowserClient } from "@supabase/ssr";
export const createClient = () =>
  createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  );

// apps/web/lib/supabase/server.ts
import { createServerClient } from "@supabase/ssr";
import { cookies } from "next/headers";
export async function createClient() {
  const cookieStore = await cookies();
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() { return cookieStore.getAll(); },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            );
          } catch {}
        },
      },
    }
  );
}

// apps/web/lib/supabase/admin.ts
import { createClient } from "@supabase/supabase-js";
export const adminClient = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY!  // NO NEXT_PUBLIC_ prefix
);
```

Inngest scaffold (verbatim from RESEARCH.md Pattern 10):

```typescript
// apps/web/inngest/client.ts
import { Inngest } from "inngest";
export const inngest = new Inngest({ id: "client-architecture" });

// apps/web/app/api/inngest/route.ts
import { serve } from "inngest/next";
import { inngest } from "@/inngest/client";
export const maxDuration = 300;
export const { GET, POST, PUT } = serve({ client: inngest, functions: [] });
```

Tailwind v4 + OKLCH theme tokens (verbatim from UI-SPEC §Color, light mode):
- --background: oklch(97% 0.008 60)
- --secondary: oklch(94% 0.010 55)
- --accent: oklch(62% 0.14 50)
- --destructive: oklch(55% 0.18 25)
- --foreground: oklch(20% 0.008 60)
- --muted-foreground: oklch(48% 0.008 60)
- --border: oklch(88% 0.010 55)
- --health-red: oklch(55% 0.20 25)
- --health-green: oklch(60% 0.14 145)

Dark mode tokens (UI-SPEC §Color, dark mode):
- --background: oklch(14% 0.010 50)
- --secondary: oklch(19% 0.012 50)
- --accent: oklch(70% 0.14 50)
- --destructive: oklch(65% 0.18 25)
- --foreground: oklch(93% 0.008 60)
- --muted-foreground: oklch(60% 0.008 55)
- --border: oklch(28% 0.012 50)
- --health-red: oklch(65% 0.22 25)
- --health-green: oklch(68% 0.14 145)

Inngest event-name constants (packages/shared/src/constants/events.ts) — exhaustive, locked here so Phase 3 functions reference identical strings:
- LEAD_NO_SHOW = "lead/no_show"
- LEAD_CALL_BOOKED = "lead/call_booked"
- LEAD_CALL_COMPLETED = "lead/call_completed"
- LEAD_REPLIED = "lead/replied"
- LEAD_BOUNCED = "lead/bounced"
- LEAD_UNSUBSCRIBED = "lead/unsubscribed"
- LEAD_MANUALLY_ENROLLED = "lead/manually_enrolled"
- DRAFT_READY = "draft/ready"
- DRAFT_APPROVED = "draft/approved"
- DRAFT_HELD = "draft/held"
- DRAFT_REGENERATE = "draft/regenerate"
- INTEGRATION_DISCONNECTED = "integration/disconnected"
- GMAIL_WATCH_RENEW = "gmail/watch_renew"

Required env vars (every variable below must appear in .env.example):
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY              # never NEXT_PUBLIC_
SUPABASE_ACCESS_TOKEN                  # for `supabase db push`
DATABASE_URL                           # Supavisor port 6543, transaction mode
NEXT_PUBLIC_APP_URL
GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET
GOOGLE_REDIRECT_URI
ANTHROPIC_API_KEY                      # ai-engine, server-only
UPSTASH_REDIS_REST_URL
UPSTASH_REDIS_REST_TOKEN
INSTAGRAM_WEBHOOK_VERIFY_TOKEN
INNGEST_EVENT_KEY
INNGEST_SIGNING_KEY
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create monorepo skeleton + workspace packages + base configs</name>
  <files>
    package.json, pnpm-workspace.yaml, turbo.json, .gitignore, .nvmrc, .env.example,
    apps/web/package.json, apps/web/next.config.ts, apps/web/tsconfig.json,
    apps/web/postcss.config.mjs, apps/web/eslint.config.mjs,
    packages/shared/package.json, packages/shared/tsconfig.json, packages/shared/src/index.ts,
    packages/shared/src/types/index.ts, packages/shared/src/constants/events.ts,
    packages/database/package.json, packages/database/tsconfig.json,
    packages/database/src/index.ts, packages/database/src/types.ts,
    packages/ai-engine/package.json, packages/ai-engine/tsconfig.json,
    packages/ai-engine/src/index.ts
  </files>
  <read_first>
    - /Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/CLAUDE.md
    - /Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.planning/phases/01-foundation/01-RESEARCH.md (Patterns 1, 2)
    - /Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.planning/phases/01-foundation/01-UI-SPEC.md (Spacing, Typography, Color)
  </read_first>
  <behavior>
    - Test 1: `pnpm install` resolves with zero errors and creates pnpm-lock.yaml at repo root
    - Test 2: `pnpm --filter web type-check` exits 0 with strict mode active
    - Test 3: `pnpm --filter web build` produces .next directory
    - Test 4: Importing `@client/ai-engine` in any file under apps/web/app where `"use client"` is present throws at module load (runtime guard)
    - Test 5: `packages/ai-engine` package.json contains `"sideEffects": false` AND a `"browser"` field that disables the entry
  </behavior>
  <action>
Create the Turborepo + pnpm monorepo structure. Use exact versions and patterns from RESEARCH.md.

**Step 1.1 — Root files:**

`package.json` (root, repo workspace manifest):
- `"name": "client-architecture"`, `"private": true`, `"version": "0.0.0"`
- `"packageManager": "pnpm@9.15.4"` (RESEARCH.md says pnpm 9.15.4)
- `"engines": { "node": ">=22" }`
- devDependencies: `"turbo": "2.9.9"`, `"typescript": "5.6.3"`, `"prettier": "3.3.3"`
- Scripts: `"build": "turbo build"`, `"dev": "turbo dev"`, `"lint": "turbo lint"`, `"type-check": "turbo type-check"`, `"test": "turbo test"`, `"db:push": "supabase db push"`, `"db:gen-types": "supabase gen types typescript --linked > packages/database/src/types.ts"`

`pnpm-workspace.yaml`:
```yaml
packages:
  - "apps/*"
  - "packages/*"
```

`turbo.json` — verbatim from RESEARCH.md Pattern 1, plus `test` and `dev` tasks:
```json
{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": [".next/**", "!.next/cache/**", "dist/**"] },
    "dev": { "cache": false, "persistent": true },
    "lint": { "dependsOn": ["^build"] },
    "type-check": { "dependsOn": ["^build"] },
    "test": { "dependsOn": ["^build"], "outputs": ["coverage/**"] }
  }
}
```

`.gitignore`: include `node_modules`, `.next`, `dist`, `.turbo`, `.env*` except `.env.example`, `coverage`, `playwright-report`, `test-results`, `*.tsbuildinfo`, `.DS_Store`.

`.nvmrc`: contents `22`.

`.env.example`: include EVERY env var from the `<interfaces>` block, one per line in `KEY=` format, with comments. CRITICAL: include this header comment at top of the SUPABASE_SERVICE_ROLE_KEY line:
```
# NEVER add NEXT_PUBLIC_ prefix to this key — server-side only.
# CI grep check (.github/workflows/ci.yml) will fail the build if violated.
SUPABASE_SERVICE_ROLE_KEY=
```
For DATABASE_URL include comment: `# MUST use Supavisor port 6543 (transaction mode) — never 5432 (INFRA-004)`.

**Step 1.2 — apps/web package:**

`apps/web/package.json`:
- `"name": "web"`, `"version": "0.0.0"`, `"private": true`
- dependencies (use exact pinned versions from `<interfaces>`):
  - `"next": "16.2.4"`, `"react": "19.0.0"`, `"react-dom": "19.0.0"`
  - `"@supabase/supabase-js": "2.105.3"`, `"@supabase/ssr": "0.10.2"`
  - `"inngest": "4.2.6"`, `"googleapis": "171.4.0"`
  - `"zod": "4.4.3"`, `"framer-motion": "12.38.0"`, `"@phosphor-icons/react": "2.1.10"`
  - `"@upstash/ratelimit": "2.0.8"`, `"@upstash/redis": "1.37.0"`
  - `"@client/shared": "workspace:*"`, `"@client/database": "workspace:*"`, `"@client/ai-engine": "workspace:*"`
  - `"sonner": "1.7.1"` (Toast)
- devDependencies:
  - `"typescript": "5.6.3"`, `"@types/node": "22.9.0"`, `"@types/react": "19.0.1"`, `"@types/react-dom": "19.0.1"`
  - `"tailwindcss": "4.2.4"`, `"@tailwindcss/postcss": "4.2.4"`, `"postcss": "8.4.49"`, `"autoprefixer": "10.4.20"`
  - `"vitest": "4.1.5"`, `"@vitejs/plugin-react": "5.0.0"`, `"happy-dom": "16.1.1"`, `"@vitest/ui": "4.1.5"`
  - `"@playwright/test": "1.59.1"`
  - `"eslint": "9.16.0"`, `"eslint-config-next": "16.2.4"`, `"@typescript-eslint/parser": "8.18.1"`, `"@typescript-eslint/eslint-plugin": "8.18.1"`
- scripts:
  - `"dev": "next dev"`, `"build": "next build"`, `"start": "next start"`
  - `"lint": "eslint ."`, `"type-check": "tsc --noEmit"`
  - `"test": "vitest run && playwright test"`, `"test:unit": "vitest run --testPathPattern='unit/'"`, `"test:integration": "vitest run --testPathPattern='integration/'"`, `"test:e2e": "playwright test"`

`apps/web/next.config.ts` — verbatim RESEARCH.md Pattern 1 (corrected for ESM):
```typescript
import type { NextConfig } from "next";
import path from "node:path";
import { fileURLToPath } from "node:url";

const __dirname = path.dirname(fileURLToPath(import.meta.url));

const config: NextConfig = {
  transpilePackages: ["@client/shared", "@client/database", "@client/ai-engine"],
  outputFileTracingRoot: path.join(__dirname, "../../"),
  experimental: { typedRoutes: true },
};

export default config;
```

`apps/web/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "preserve",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "skipLibCheck": true,
    "incremental": true,
    "allowJs": false,
    "noEmit": true,
    "plugins": [{ "name": "next" }],
    "baseUrl": ".",
    "paths": { "@/*": ["./*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

`apps/web/postcss.config.mjs`:
```javascript
export default { plugins: { "@tailwindcss/postcss": {} } };
```

`apps/web/eslint.config.mjs` — flat config, MUST include `no-console` rule with custom token/secret patterns (COMPLY-009):
```javascript
import next from "eslint-config-next";

export default [
  ...next,
  {
    rules: {
      "@typescript-eslint/no-explicit-any": "error",
      "no-console": ["error", { "allow": ["warn", "error"] }],
      "no-restricted-syntax": [
        "error",
        {
          "selector": "CallExpression[callee.object.name='console'][callee.property.name=/^(log|info|debug)$/]",
          "message": "console.log/info/debug forbidden — sensitive data leaks (COMPLY-009). Use a structured logger or remove."
        }
      ]
    }
  }
];
```

**Step 1.3 — packages/shared:**

`packages/shared/package.json` — verbatim from RESEARCH.md Pattern 1 (with version):
```json
{
  "name": "@client/shared",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types/index.ts",
    "./validators": "./src/validators/index.ts",
    "./constants": "./src/constants/events.ts"
  },
  "dependencies": { "zod": "4.4.3" }
}
```

`packages/shared/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noEmit": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "isolatedModules": true,
    "declaration": true
  },
  "include": ["src/**/*.ts"]
}
```

`packages/shared/src/index.ts`:
```typescript
export * from "./types";
export * from "./validators";
export * as Events from "./constants/events";
```

`packages/shared/src/types/index.ts` — declare placeholder types now, populated by Plan 02 type generation:
```typescript
// Re-exported from @client/database/types after `supabase gen types`
// Phase 1 Plan 02 generates the live types. Until then these are placeholders
// for downstream task type imports.
export type TLeadStatus =
  | "identified" | "call_booked" | "no_show" | "call_completed"
  | "in_sequence" | "replied" | "converted" | "closed"
  | "unsubscribed" | "do_not_contact" | "bounced";

export type TLeadSource =
  | "calendly" | "cal_com" | "acuity" | "setmore" | "square"
  | "ms_bookings" | "tidycal" | "manual" | "gmail_detected"
  | "instagram_detected" | "referral";

export type TDraftStatus =
  | "pending" | "approved" | "edited" | "sent" | "held" | "cancelled";

export type TIntegrationProvider =
  | "gmail" | "calendly" | "cal_com" | "acuity" | "setmore"
  | "square" | "ms_bookings" | "tidycal" | "slack" | "twilio"
  | "instagram";
```

`packages/shared/src/constants/events.ts` — verbatim event names from `<interfaces>` block:
```typescript
export const LEAD_NO_SHOW = "lead/no_show";
export const LEAD_CALL_BOOKED = "lead/call_booked";
export const LEAD_CALL_COMPLETED = "lead/call_completed";
export const LEAD_REPLIED = "lead/replied";
export const LEAD_BOUNCED = "lead/bounced";
export const LEAD_UNSUBSCRIBED = "lead/unsubscribed";
export const LEAD_MANUALLY_ENROLLED = "lead/manually_enrolled";
export const DRAFT_READY = "draft/ready";
export const DRAFT_APPROVED = "draft/approved";
export const DRAFT_HELD = "draft/held";
export const DRAFT_REGENERATE = "draft/regenerate";
export const INTEGRATION_DISCONNECTED = "integration/disconnected";
export const GMAIL_WATCH_RENEW = "gmail/watch_renew";
```

(Validators directory is created in Step 1.4 but file added in Plan 04 — for now create stub `packages/shared/src/validators/index.ts` exporting `export {};`.)

**Step 1.4 — packages/database:**

`packages/database/package.json`:
```json
{
  "name": "@client/database",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/types.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts"
  }
}
```

`packages/database/src/index.ts`:
```typescript
export * from "./types";
```

`packages/database/src/types.ts` — placeholder until Plan 02 runs `supabase gen types`:
```typescript
// Generated by `pnpm db:gen-types` after Plan 02 schema migration.
// Placeholder so downstream packages compile until then.
export type Database = Record<string, never>;
export type Tables = Record<string, never>;
```

`packages/database/tsconfig.json`: same content as packages/shared/tsconfig.json.

**Step 1.5 — packages/ai-engine (server-only enforcement):**

`packages/ai-engine/package.json` — RESEARCH.md Pattern 2 (Layers 1 + 3):
```json
{
  "name": "@client/ai-engine",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "browser": { "./src/index.ts": false },
  "exports": { ".": "./src/index.ts" }
}
```

`packages/ai-engine/src/index.ts` — RESEARCH.md Pattern 2 Layer 2 runtime guard, then exports:
```typescript
// INFRA-008: Server-only package. Hard-fail if imported in browser context.
if (typeof window !== "undefined") {
  throw new Error(
    "@client/ai-engine must not be imported in client-side code. " +
    "This package wraps Anthropic API calls (server-side only)."
  );
}

// Phase 2 implementation lives here. Phase 1 ships only the guard.
export const AI_ENGINE_VERSION = "0.0.1-scaffold";
```

`packages/ai-engine/tsconfig.json`: same as packages/shared.

**Step 1.6 — Run install:**
Run `pnpm install` from the repo root. Verify pnpm-lock.yaml is created and that `apps/web/node_modules/@client/shared`, `@client/database`, `@client/ai-engine` are symlinked.

**Step 1.7 — Type-check the workspace:**
Run `pnpm --filter web type-check`. Must exit 0.
  </action>
  <verify>
    <automated>
      cd /Users/augustavesterlyngvilsoe/Desktop/Claude\ code/Program\ for\ coaches && \
      pnpm install --frozen-lockfile=false && \
      pnpm --filter web type-check && \
      test -L apps/web/node_modules/@client/shared && \
      test -L apps/web/node_modules/@client/ai-engine && \
      grep -q '"sideEffects": false' packages/ai-engine/package.json && \
      grep -q 'typeof window' packages/ai-engine/src/index.ts && \
      grep -q "transpilePackages" apps/web/next.config.ts && \
      grep -q '"strict": true' apps/web/tsconfig.json && \
      grep -q "no-console" apps/web/eslint.config.mjs
    </automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` exits 0 and produces pnpm-lock.yaml at repo root
    - `pnpm --filter web type-check` exits 0
    - `apps/web/node_modules/@client/shared` is a symlink (workspace resolution working)
    - `apps/web/node_modules/@client/ai-engine` is a symlink
    - `packages/ai-engine/package.json` contains `"sideEffects": false`
    - `packages/ai-engine/src/index.ts` contains `typeof window`
    - `apps/web/next.config.ts` contains `transpilePackages`
    - `apps/web/tsconfig.json` contains `"strict": true`
    - `apps/web/eslint.config.mjs` declares the `no-console` error rule
  </acceptance_criteria>
  <done>
    Repo skeleton built. `pnpm install` and `pnpm --filter web type-check` both green. Workspace packages resolve via symlinks. ai-engine has runtime guard + browser field + sideEffects flag.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Tailwind v4 + shadcn/ui new-york + Supabase clients + Inngest serve + Instagram webhook scaffold + middleware</name>
  <files>
    apps/web/app/globals.css, apps/web/app/layout.tsx, apps/web/app/page.tsx,
    apps/web/components.json, apps/web/middleware.ts,
    apps/web/lib/supabase/browser.ts, apps/web/lib/supabase/server.ts, apps/web/lib/supabase/admin.ts,
    apps/web/inngest/client.ts, apps/web/app/api/inngest/route.ts,
    apps/web/app/api/webhooks/instagram/route.ts
  </files>
  <read_first>
    - /Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.planning/phases/01-foundation/01-RESEARCH.md (Patterns 3, 4, 10; Instagram scaffold section)
    - /Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.planning/phases/01-foundation/01-UI-SPEC.md (Color § for OKLCH tokens)
    - /Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/CLAUDE.md (security non-negotiables)
  </read_first>
  <behavior>
    - Test 1: `apps/web/lib/supabase/server.ts` exports `createClient` that calls `createServerClient` from `@supabase/ssr` with `cookies` interface using `getAll`/`setAll`
    - Test 2: `apps/web/lib/supabase/admin.ts` constructs the admin client using `process.env.SUPABASE_SERVICE_ROLE_KEY` (NOT `NEXT_PUBLIC_`)
    - Test 3: `apps/web/middleware.ts` calls `supabase.auth.getUser()` (NEVER `getSession()`) and redirects unauthenticated requests to `/login` for protected routes
    - Test 4: `apps/web/app/api/inngest/route.ts` exports `GET`, `POST`, `PUT`, and `maxDuration = 300`
    - Test 5: `GET /api/webhooks/instagram?hub.mode=subscribe&hub.verify_token=$INSTAGRAM_WEBHOOK_VERIFY_TOKEN&hub.challenge=test123` returns body `test123` with 200; wrong token returns 403
    - Test 6: `apps/web/app/globals.css` defines OKLCH variables exactly as in UI-SPEC for both light and dark
  </behavior>
  <action>
**Step 2.1 — Tailwind v4 globals + shadcn/ui init:**

Run `pnpm dlx shadcn@4.7.0 init --style new-york --base-color neutral --css-variables` from `apps/web/`. Confirm `apps/web/components.json` is created and contains `"style": "new-york"`. If init prompts for choices, accept TypeScript, src directory: no, app directory: yes, alias: `@/*`.

After init, REPLACE `apps/web/app/globals.css` with this exact content (Tailwind v4 + UI-SPEC OKLCH from `<interfaces>`):

```css
@import "tailwindcss";

@custom-variant dark (&:where(.dark, .dark *));

:root {
  /* Light mode — UI-SPEC §Color */
  --background: oklch(97% 0.008 60);
  --foreground: oklch(20% 0.008 60);
  --card: oklch(94% 0.010 55);
  --card-foreground: oklch(20% 0.008 60);
  --popover: oklch(94% 0.010 55);
  --popover-foreground: oklch(20% 0.008 60);
  --primary: oklch(62% 0.14 50);
  --primary-foreground: oklch(97% 0.008 60);
  --secondary: oklch(94% 0.010 55);
  --secondary-foreground: oklch(20% 0.008 60);
  --muted: oklch(94% 0.010 55);
  --muted-foreground: oklch(48% 0.008 60);
  --accent: oklch(62% 0.14 50);
  --accent-foreground: oklch(97% 0.008 60);
  --destructive: oklch(55% 0.18 25);
  --destructive-foreground: oklch(97% 0.008 60);
  --border: oklch(88% 0.010 55);
  --input: oklch(88% 0.010 55);
  --ring: oklch(62% 0.14 50);
  --health-red: oklch(55% 0.20 25);
  --health-green: oklch(60% 0.14 145);
  --radius: 1rem;
  --bg-image: none;
}

.dark {
  /* Dark mode — UI-SPEC §Color */
  --background: oklch(14% 0.010 50);
  --foreground: oklch(93% 0.008 60);
  --card: oklch(19% 0.012 50);
  --card-foreground: oklch(93% 0.008 60);
  --popover: oklch(19% 0.012 50);
  --popover-foreground: oklch(93% 0.008 60);
  --primary: oklch(70% 0.14 50);
  --primary-foreground: oklch(14% 0.010 50);
  --secondary: oklch(19% 0.012 50);
  --secondary-foreground: oklch(93% 0.008 60);
  --muted: oklch(19% 0.012 50);
  --muted-foreground: oklch(60% 0.008 55);
  --accent: oklch(70% 0.14 50);
  --accent-foreground: oklch(14% 0.010 50);
  --destructive: oklch(65% 0.18 25);
  --destructive-foreground: oklch(14% 0.010 50);
  --border: oklch(28% 0.012 50);
  --input: oklch(28% 0.012 50);
  --ring: oklch(70% 0.14 50);
  --health-red: oklch(65% 0.22 25);
  --health-green: oklch(68% 0.14 145);
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-card: var(--card);
  --color-card-foreground: var(--card-foreground);
  --color-popover: var(--popover);
  --color-popover-foreground: var(--popover-foreground);
  --color-primary: var(--primary);
  --color-primary-foreground: var(--primary-foreground);
  --color-secondary: var(--secondary);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-muted: var(--muted);
  --color-muted-foreground: var(--muted-foreground);
  --color-accent: var(--accent);
  --color-accent-foreground: var(--accent-foreground);
  --color-destructive: var(--destructive);
  --color-destructive-foreground: var(--destructive-foreground);
  --color-border: var(--border);
  --color-input: var(--input);
  --color-ring: var(--ring);
  --color-health-red: var(--health-red);
  --color-health-green: var(--health-green);
  --radius-lg: var(--radius);
  --radius-md: calc(var(--radius) - 4px);
  --radius-sm: calc(var(--radius) - 8px);
  --font-sans: var(--font-geist-sans), ui-sans-serif, system-ui;
  --font-mono: var(--font-geist-mono), ui-monospace;
}

body {
  min-height: 100dvh;
  background-color: var(--background);
  color: var(--foreground);
  font-family: var(--font-sans);
  background-image: var(--bg-image);
  background-size: cover;
  background-attachment: fixed;
}
```

**Step 2.2 — Root layout + Geist fonts:**

`apps/web/app/layout.tsx`:
```typescript
import type { Metadata } from "next";
import { GeistSans } from "geist/font/sans";
import { GeistMono } from "geist/font/mono";
import { Toaster } from "sonner";
import "./globals.css";

export const metadata: Metadata = {
  title: "The Client Architecture",
  description: "AI follow-up system for coaches",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${GeistSans.variable} ${GeistMono.variable} antialiased`}>
        {children}
        <Toaster position="bottom-right" richColors closeButton />
      </body>
    </html>
  );
}
```

Add `"geist": "1.3.1"` to `apps/web/package.json` dependencies. Run `pnpm install`.

`apps/web/app/page.tsx` — minimal placeholder; Plan 03/06 replace this:
```typescript
import { redirect } from "next/navigation";

export default function Home() {
  redirect("/login");
}
```

**Step 2.3 — Supabase clients (verbatim from `<interfaces>` block):**

Create the three files exactly as written in `<interfaces>` — `apps/web/lib/supabase/{browser,server,admin}.ts`. Do NOT deviate from the cookie handling pattern. CRITICAL: `admin.ts` must use `process.env.SUPABASE_SERVICE_ROLE_KEY` with NO `NEXT_PUBLIC_` prefix. Add header comment:
```typescript
// SERVER-SIDE ONLY. Importing this in a "use client" component
// will leak the service role key to the browser bundle.
// CI grep check (.github/workflows/ci.yml) blocks NEXT_PUBLIC_*SERVICE_ROLE.
```

**Step 2.4 — Middleware (RESEARCH.md Pattern 4 verbatim):**

`apps/web/middleware.ts` — copy RESEARCH.md Pattern 4 verbatim. CRITICAL details:
- Use `getUser()`, never `getSession()` (RESEARCH.md anti-pattern)
- Admin gate: check `user?.app_metadata?.role === "admin"` for `/admin/*`
- Protected routes: redirect to `/login` if no user for `/leads`, `/drafts`, `/settings`, `/dashboard`
- Auth pages (`/login`, `/invite`): allow without user
- Public webhook routes (`/api/webhooks/*`, `/api/inngest`, `/api/auth/gmail/*`): bypass middleware completely

Update matcher to:
```typescript
export const config = {
  matcher: ["/((?!_next/static|_next/image|favicon.ico|api/inngest|api/webhooks|api/auth/gmail).*)"],
};
```

**Step 2.5 — Inngest scaffold (RESEARCH.md Pattern 10 verbatim):**

`apps/web/inngest/client.ts` — copy verbatim from `<interfaces>` block.

`apps/web/app/api/inngest/route.ts` — copy verbatim from `<interfaces>` block. The `export const maxDuration = 300;` line MUST be present (RESEARCH.md Pitfall 4: Vercel default 10s timeout kills Inngest).

**Step 2.6 — Instagram webhook verification scaffold (INFRA-010, RESEARCH.md Instagram section verbatim):**

`apps/web/app/api/webhooks/instagram/route.ts` — copy verbatim from RESEARCH.md "Code scaffold (Phase 1 — no implementation)" section. The GET handler validates `hub.verify_token` against `process.env.INSTAGRAM_WEBHOOK_VERIFY_TOKEN` and returns `hub.challenge` body on match, 403 otherwise. POST returns 200.
  </action>
  <verify>
    <automated>
      cd /Users/augustavesterlyngvilsoe/Desktop/Claude\ code/Program\ for\ coaches/apps/web && \
      grep -q "createBrowserClient" lib/supabase/browser.ts && \
      grep -q "createServerClient" lib/supabase/server.ts && \
      grep -q "SUPABASE_SERVICE_ROLE_KEY" lib/supabase/admin.ts && \
      grep -v '^//' lib/supabase/admin.ts | grep -c "NEXT_PUBLIC_.*SERVICE_ROLE\|NEXT_PUBLIC_.*service_role" | grep -q "^0$" && \
      grep -q "getUser" middleware.ts && \
      ! grep -q "getSession" middleware.ts && \
      grep -q "maxDuration = 300" app/api/inngest/route.ts && \
      grep -q "GET, POST, PUT" app/api/inngest/route.ts && \
      grep -q "hub.challenge" app/api/webhooks/instagram/route.ts && \
      grep -q "INSTAGRAM_WEBHOOK_VERIFY_TOKEN" app/api/webhooks/instagram/route.ts && \
      grep -q "oklch(62% 0.14 50)" app/globals.css && \
      grep -q "oklch(70% 0.14 50)" app/globals.css && \
      grep -q '"style": "new-york"' components.json && \
      pnpm type-check
    </automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/lib/supabase/browser.ts` calls `createBrowserClient`
    - `apps/web/lib/supabase/server.ts` calls `createServerClient` with `cookies` `getAll`/`setAll` interface
    - `apps/web/lib/supabase/admin.ts` uses `process.env.SUPABASE_SERVICE_ROLE_KEY` (no `NEXT_PUBLIC_` prefix anywhere — exclude comment lines starting with `//`)
    - `apps/web/middleware.ts` contains `getUser` and does NOT contain `getSession`
    - `apps/web/app/api/inngest/route.ts` contains `maxDuration = 300` AND exports `GET, POST, PUT`
    - `apps/web/app/api/webhooks/instagram/route.ts` validates `hub.verify_token` against env and echoes `hub.challenge`
    - `apps/web/app/globals.css` declares OKLCH tokens for both `:root` and `.dark` (light mode `oklch(62% 0.14 50)` accent + dark `oklch(70% 0.14 50)`)
    - `apps/web/components.json` style is `"new-york"` (Pitfall 7)
    - `pnpm --filter web type-check` exits 0
  </acceptance_criteria>
  <done>
    Tailwind v4 + shadcn/ui new-york preset wired with full OKLCH palette from UI-SPEC. Three Supabase client factories created (browser, server, admin). Middleware refreshes auth tokens via `getUser()` and gates `/admin` + `/leads` + `/drafts` + `/settings`. Inngest serve scaffold live at `/api/inngest` with `maxDuration = 300`. Instagram webhook verification scaffold responds correctly to Meta verification pings (INFRA-010).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test infrastructure (Vitest + Playwright configs + every Phase 1 test stub) + CI workflow</name>
  <files>
    apps/web/vitest.config.ts, apps/web/playwright.config.ts,
    apps/web/tests/setup.ts,
    apps/web/tests/unit/validators.test.ts, apps/web/tests/unit/state-machine.test.ts,
    apps/web/tests/unit/scope-validation.test.ts, apps/web/tests/unit/invalid-grant.test.ts,
    apps/web/tests/integration/rls.test.ts, apps/web/tests/integration/vault.test.ts,
    apps/web/tests/integration/gmail-oauth.test.ts, apps/web/tests/integration/vault-storage.test.ts,
    apps/web/tests/integration/do-not-contact.test.ts, apps/web/tests/integration/state-transitions.test.ts,
    apps/web/tests/integration/realtime-drafts.test.ts, apps/web/tests/integration/ratelimit.test.ts,
    apps/web/tests/e2e/lead-create.spec.ts, apps/web/tests/e2e/lead-profile.spec.ts,
    apps/web/tests/e2e/lead-timeline.spec.ts, apps/web/tests/e2e/lead-notes.spec.ts,
    apps/web/tests/e2e/lead-list.spec.ts, apps/web/tests/e2e/admin-access.spec.ts,
    apps/web/tests/e2e/admin-dashboard.spec.ts, apps/web/tests/e2e/invite-coach.spec.ts,
    apps/web/tests/e2e/gmail-connect.spec.ts, apps/web/tests/e2e/health-card.spec.ts,
    .github/workflows/ci.yml
  </files>
  <read_first>
    - /Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.planning/phases/01-foundation/01-VALIDATION.md (the entire Per-Task Verification Map)
    - /Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.planning/phases/01-foundation/01-RESEARCH.md (Validation Architecture, CI grep checks)
  </read_first>
  <behavior>
    - Test 1: `pnpm --filter web vitest run --testPathPattern='unit/'` runs ALL unit tests (each currently `it.todo` placeholder), exits 0 within 10s
    - Test 2: `pnpm --filter web vitest run` discovers integration test files, exits 0 (todos pass)
    - Test 3: `pnpm --filter web playwright test --list` lists every spec file
    - Test 4: CI workflow `.github/workflows/ci.yml` includes a step that greps for `NEXT_PUBLIC_.*service_role` and fails on match
    - Test 5: Same CI workflow runs `pnpm --filter web type-check`, `pnpm --filter web lint`, `pnpm --filter web vitest run`
  </behavior>
  <action>
**Step 3.1 — Vitest config:**

`apps/web/vitest.config.ts`:
```typescript
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";
import path from "node:path";

export default defineConfig({
  plugins: [react()],
  test: {
    environment: "happy-dom",
    setupFiles: ["./tests/setup.ts"],
    include: ["tests/unit/**/*.test.ts", "tests/integration/**/*.test.ts"],
    globals: true,
    testTimeout: 30000,
  },
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "."),
      "@client/shared": path.resolve(__dirname, "../../packages/shared/src"),
      "@client/database": path.resolve(__dirname, "../../packages/database/src"),
    },
  },
});
```

`apps/web/tests/setup.ts`:
```typescript
import { vi } from "vitest";

// Mock next/headers for unit tests (not available outside route handlers)
vi.mock("next/headers", () => ({
  cookies: () => ({
    getAll: () => [],
    set: () => {},
  }),
}));

// Default test env vars (override per-test as needed)
process.env.NEXT_PUBLIC_SUPABASE_URL ??= "https://test.supabase.co";
process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY ??= "test-anon-key";
process.env.SUPABASE_SERVICE_ROLE_KEY ??= "test-service-role-key";
process.env.INSTAGRAM_WEBHOOK_VERIFY_TOKEN ??= "test-verify-token";
```

**Step 3.2 — Playwright config:**

`apps/web/playwright.config.ts`:
```typescript
import { defineConfig, devices } from "@playwright/test";

export default defineConfig({
  testDir: "./tests/e2e",
  timeout: 30_000,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : 4,
  reporter: process.env.CI ? "github" : "list",
  use: {
    baseURL: process.env.PLAYWRIGHT_BASE_URL ?? "http://localhost:3000",
    trace: "on-first-retry",
    screenshot: "only-on-failure",
  },
  projects: [
    { name: "chromium", use: { ...devices["Desktop Chrome"] } },
  ],
  webServer: process.env.CI ? undefined : {
    command: "pnpm dev",
    url: "http://localhost:3000",
    timeout: 120_000,
    reuseExistingServer: true,
  },
});
```

**Step 3.3 — Unit test stubs:**

For each unit test file listed in VALIDATION.md, create a stub of the following form (replace `{REQ}` with the requirement ID it covers):

`apps/web/tests/unit/validators.test.ts`:
```typescript
import { describe, it } from "vitest";

describe("INFRA-005: Zod validators", () => {
  it.todo("CreateLeadSchema rejects missing email — Plan 04 implements");
  it.todo("CreateLeadSchema rejects invalid source enum — Plan 04 implements");
  it.todo("CreateLeadSchema accepts valid manual lead — Plan 04 implements");
});
```

`apps/web/tests/unit/state-machine.test.ts`:
```typescript
import { describe, it } from "vitest";

describe("STATE-001: Lead state machine enum", () => {
  it.todo("All 11 lead states present in TS type — Plan 04 implements");
  it.todo("Terminal states (do_not_contact, unsubscribed, converted, closed, bounced) flagged — Plan 04 implements");
});
```

`apps/web/tests/unit/scope-validation.test.ts`:
```typescript
import { describe, it } from "vitest";

describe("HEALTH-007: Gmail OAuth scope validation", () => {
  it.todo("Under-scoped token blocks connection — Plan 05 implements");
  it.todo("All required scopes granted marks connected — Plan 05 implements");
});
```

`apps/web/tests/unit/invalid-grant.test.ts`:
```typescript
import { describe, it } from "vitest";

describe("HEALTH-004: invalid_grant handler", () => {
  it.todo("invalid_grant marks integration disconnected — Plan 05 implements");
  it.todo("invalid_grant halts active sequences — Plan 05 implements");
});
```

**Step 3.4 — Integration test stubs:**

Apply the same pattern for every integration test file in VALIDATION.md. Each file: a single `describe` named with the requirement ID, with 1–3 `it.todo` placeholders naming what the implementing plan delivers. Files: `rls.test.ts` (INFRA-001 + ADMIN-005 + VOICE-006), `vault.test.ts` (INFRA-003), `gmail-oauth.test.ts` (GMAIL-002), `vault-storage.test.ts` (GMAIL-003), `do-not-contact.test.ts` (STATE-007), `state-transitions.test.ts` (STATE-009), `realtime-drafts.test.ts` (DRAFT-012), `ratelimit.test.ts` (INFRA-009).

**Step 3.5 — E2E test stubs:**

Each E2E spec file should be a single `test()` body using `test.fixme(...)` to mark as not yet implemented, e.g.:

`apps/web/tests/e2e/lead-create.spec.ts`:
```typescript
import { test } from "@playwright/test";

test.fixme("LEAD-001: Coach creates a lead via AddLead sheet — Plan 04 implements", async ({ page }) => {
  // Plan 04 task 04-01 implements
});
```

Apply identically for all e2e specs in VALIDATION.md. `test.fixme` ensures the runner discovers + lists each spec but skips execution (returns green).

**Step 3.6 — CI workflow:**

`.github/workflows/ci.yml`:
```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  security-grep:
    name: Security grep checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Block NEXT_PUBLIC_ on service role key (INFRA-002)
        run: |
          if grep -rn "NEXT_PUBLIC_[A-Z_]*SERVICE_ROLE\|NEXT_PUBLIC_[A-Z_]*service_role" \
              --include="*.ts" --include="*.tsx" --include="*.env*" \
              --exclude-dir=node_modules --exclude-dir=.next \
              .; then
            echo "FAIL: Service role key prefixed with NEXT_PUBLIC_ — security violation"
            exit 1
          fi
      - name: Block port 5432 in env files (INFRA-004)
        run: |
          if grep -rn ":5432" .env.example 2>/dev/null; then
            echo "FAIL: DATABASE_URL must use Supavisor port 6543 (transaction mode), not 5432"
            exit 1
          fi

  type-check:
    name: TypeScript type-check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: 9.15.4 }
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: pnpm }
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter web type-check

  lint:
    name: ESLint (no-console enforcement)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: 9.15.4 }
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: pnpm }
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter web lint

  unit-tests:
    name: Unit + integration tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: 9.15.4 }
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: pnpm }
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter web vitest run

  build:
    name: Next.js build + ai-engine bundle audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: 9.15.4 }
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: pnpm }
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter web build
        env:
          NEXT_PUBLIC_SUPABASE_URL: https://placeholder.supabase.co
          NEXT_PUBLIC_SUPABASE_ANON_KEY: placeholder
          SUPABASE_SERVICE_ROLE_KEY: placeholder
      - name: Verify ai-engine absent from client bundle (INFRA-008, T-1-01)
        run: |
          if grep -rE "@client/ai-engine|client-architecture-ai-engine" apps/web/.next/static/ 2>/dev/null; then
            echo "FAIL: ai-engine code leaked to client bundle"
            exit 1
          fi
          echo "PASS: ai-engine clean"
```
  </action>
  <verify>
    <automated>
      cd /Users/augustavesterlyngvilsoe/Desktop/Claude\ code/Program\ for\ coaches/apps/web && \
      pnpm vitest run --testPathPattern='unit/' && \
      pnpm vitest run && \
      pnpm playwright test --list && \
      cd /Users/augustavesterlyngvilsoe/Desktop/Claude\ code/Program\ for\ coaches && \
      grep -q "NEXT_PUBLIC_\[A-Z_\]\*SERVICE_ROLE" .github/workflows/ci.yml && \
      grep -q "ai-engine" .github/workflows/ci.yml && \
      grep -q "type-check" .github/workflows/ci.yml && \
      test -f apps/web/tests/unit/validators.test.ts && \
      test -f apps/web/tests/integration/rls.test.ts && \
      test -f apps/web/tests/e2e/lead-create.spec.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web vitest run` exits 0 (all unit + integration test files discovered, todos pass)
    - `pnpm --filter web playwright test --list` lists 10 spec files (one per VALIDATION.md row)
    - `.github/workflows/ci.yml` has 5 jobs: security-grep, type-check, lint, unit-tests, build
    - The `security-grep` job blocks `NEXT_PUBLIC_.*SERVICE_ROLE` and `:5432` in env files
    - The `build` job greps `apps/web/.next/static/` for ai-engine leakage
    - Every test file from VALIDATION.md "Wave 0 Requirements" exists
  </acceptance_criteria>
  <done>
    Test infrastructure complete: Vitest + Playwright configs, setup file with env defaults and next/headers mock, every Phase 1 test file stubbed with `it.todo`/`test.fixme`. CI workflow enforces type-check, lint (no-console), unit/integration tests, build, and the two grep guards (NEXT_PUBLIC_ service role + port 5432). Plans 02–07 can now `it.todo → it()` their assigned tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js server | Coach cookie session traverses; admin gate enforced in middleware |
| Next.js client bundle → packages/ai-engine | MUST be impossible: server-only package |
| Vercel function → Supabase | Service role key crosses; never exposed client-side |
| External webhook caller → /api/webhooks/instagram | Untrusted; verified via shared secret token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure / Elevation of Privilege | packages/ai-engine + service role key | mitigate | Three-layer defense: (1) `packages/ai-engine` `sideEffects: false` + `browser` field disables client import; (2) runtime guard `if (typeof window !== "undefined") throw` in `src/index.ts`; (3) CI build job greps `.next/static/` for `@client/ai-engine`. Service role key: `lib/supabase/admin.ts` reads `process.env.SUPABASE_SERVICE_ROLE_KEY` (no `NEXT_PUBLIC_`); CI grep job blocks regression. |
| T-1-04 | Elevation of Privilege | Admin route gate | mitigate | `apps/web/middleware.ts` checks `user?.app_metadata?.role === "admin"` for `/admin/*` paths and redirects to `/login` otherwise. Defense-in-depth via `app/admin/layout.tsx` server-side check (Plan 07). |

(T-1-02, T-1-03, T-1-05, T-1-06 are scaffolded here via test stubs; concrete mitigations live in Plans 02 and 05.)
</threat_model>

<verification>
- `pnpm install` exits 0; pnpm-lock.yaml created
- `pnpm --filter web type-check` exits 0
- `pnpm --filter web build` exits 0
- `pnpm --filter web vitest run` exits 0 (all stubs pass via todo)
- `pnpm --filter web playwright test --list` lists 10 specs
- `grep -rn "NEXT_PUBLIC_.*SERVICE_ROLE" --include='*.ts' --include='*.env*' .` returns no matches
- `grep ':5432' .env.example` returns no matches (Supavisor 6543 only)
- `apps/web/components.json` shows `"style": "new-york"`
- `apps/web/app/api/inngest/route.ts` contains both `maxDuration = 300` and `GET, POST, PUT`
- `packages/ai-engine/src/index.ts` contains `typeof window !== "undefined"` runtime guard
- `apps/web/middleware.ts` uses `getUser()` and never `getSession()`
</verification>

<success_criteria>
- Repo skeleton in place: apps/web + 3 workspace packages
- Tailwind v4 + shadcn/ui new-york preset wired with full UI-SPEC OKLCH palette (light + dark)
- Three Supabase clients (browser, server, admin) following @supabase/ssr patterns from RESEARCH.md
- Inngest serve scaffold at /api/inngest with maxDuration = 300
- Instagram webhook verification scaffold (INFRA-010) responding to GET hub.challenge correctly
- Test infrastructure: Vitest + Playwright configs + every Phase 1 test stubbed
- CI workflow with NEXT_PUBLIC_ guard, port 5432 guard, type-check, lint, vitest, build, ai-engine bundle audit
- ai-engine three-layer guard active (browser field + runtime guard + CI build audit)
- All requirements addressed: INFRA-006, INFRA-007, INFRA-008, INFRA-009, INFRA-010, COMPLY-009
</success_criteria>

<output>
After completion, create `/Users/augustavesterlyngvilsoe/Desktop/Claude code/Program for coaches/.planning/phases/01-foundation/01-01-SUMMARY.md` documenting:
- Workspace structure (apps/web + 3 packages) with package versions actually installed
- Confirmed file locations of all Supabase clients, middleware, Inngest, Instagram scaffold
- CI workflow gates (which checks block which threat IDs)
- Test stub coverage map (which spec/test file maps to which req ID)
- Any deviations from RESEARCH.md (with reason) — ideally none
</output>
